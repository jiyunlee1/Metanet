projection implementation in class zbp_mcdar_c_sales_acct unique;
strict ( 2 );
use draft;

define behavior for ZMCDAR_C_SALES_ACCT alias sales_acct
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Resume;
  use action Activate;
  use action Discard;
  use action Prepare;
}