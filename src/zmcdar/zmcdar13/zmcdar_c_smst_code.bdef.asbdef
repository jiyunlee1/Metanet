projection;
strict ( 2 );
use draft;

define behavior for ZMCDAR_C_SMST_CODE alias SmartStore
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Resume;
  use action Activate;
  use action Discard;
  use action Prepare;

  use function validation_check;
  use function excel_validation;
//  use function validation_excel;
}