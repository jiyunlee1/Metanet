projection;
strict ( 2 );
use draft;
use side effects;

define behavior for ZMCDAR_C_DEF_SMST alias DefSmartStore
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}